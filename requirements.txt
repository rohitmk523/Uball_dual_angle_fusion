# Dual Angle Fusion - Basketball Shot Detection
# Python 3.8+

# Core dependencies (shared with both angle systems)
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0
supabase>=2.0.0

# Dual-angle fusion dependencies
scipy>=1.10.0
filterpy>=1.4.5

# Audio sync dependencies
# Note: Requires ffmpeg to be installed on system (brew install ffmpeg on macOS)
