# Dual Angle Fusion - Basketball Shot Detection
# Python 3.8+

# Core dependencies (shared with both angle systems)
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0
supabase>=2.0.0

# Note: No additional dependencies needed for fusion
# The fusion script only works with JSON results from the individual systems
